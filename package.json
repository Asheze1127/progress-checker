{
  "name": "@progress-checker/workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "format": "pnpm -r --if-present run format && biome format --write package.json biome.json",
    "format:check": "pnpm -r --if-present run format:check && biome format package.json biome.json",
    "lint": "pnpm -r --if-present run lint",
    "lint:fix": "pnpm -r --if-present run lint:fix",
    "test": "pnpm -r --if-present run test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8"
  }
}
