{
  "name": "@progress-checker/workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "infrastructure/cdk"
  ],
  "scripts": {
    "format": "npm run format --workspaces --if-present && biome format --write package.json biome.json",
    "format:check": "npm run format:check --workspaces --if-present && biome format package.json biome.json",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8"
  }
}
